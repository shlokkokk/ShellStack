import type { Tool } from '../toolTypes';

export const reverseEngineeringTools: Tool[] = [
  {
    id: 'ghidra',
    name: 'Ghidra',
    description: 'Free, open-source Software Reverse Engineering suite developed by the NSA. Features a powerful decompiler that reconstructs C/C++ source from binaries, supports 30+ CPU architectures, and includes a collaborative server for team-based analysis.',
    category: 'reverse-engineering',
    difficulty: 'advanced',
    tags: ['disassembler', 'decompiler', 'sre', 'free', 'nsa', 'must-know'],
    commands: [
      { command: 'ghidra', description: 'Launch the Ghidra GUI — create a project, then import your binary for analysis' },
      { command: 'analyzeHeadless /path/to/project myProject -import binary.exe', description: 'Run analysis in headless mode (no GUI) — great for batch processing multiple binaries' },
      { command: 'analyzeHeadless /path/to/project myProject -import binary.exe -postScript ExportDecompiled.java', description: 'Headless analysis with a post-processing Ghidra script for automated output' },
      { command: 'analyzeHeadless /path/to/project myProject -process binary.exe -analysisTimeoutPerFile 600', description: 'Set a 10-minute timeout for analysis of very large binaries' },
    ],
    whenToUse: [
      'For complex binary reverse engineering when IDA Pro is not available (free alternative)',
      'To decompile binaries into readable C pseudo-code for vulnerability research',
      'When analyzing obfuscated malware — Ghidra auto-detects crypto constants and strings',
      'For firmware analysis of IoT devices (supports ARM, MIPS, PPC, etc.)',
      'When collaborating with a team — Ghidra Server enables shared analysis projects',
    ],
    commonFlags: [
      { flag: '-import', description: 'Import a binary file into the project' },
      { flag: '-process', description: 'Process an already-imported binary' },
      { flag: '-postScript', description: 'Run a Ghidra script after analysis completes' },
      { flag: '-analysisTimeoutPerFile', description: 'Max seconds for auto-analysis per file' },
      { flag: '-recursive', description: 'Import all files in a directory recursively' },
    ],
    relatedTools: ['ida-pro', 'binary-ninja', 'radare2', 'cutter'],
    installation: 'Download from https://ghidra-sre.org — requires Java 17+',
    website: 'https://ghidra-sre.org',
  },
  {
    id: 'radare2',
    name: 'Radare2',
    description: 'Complete CLI-based reverse engineering framework. Disassembles, debugs, patches, and analyzes binaries entirely from the terminal. Extremely powerful scripting API (r2pipe) for automation. Steep learning curve but unmatched for speed and scriptability.',
    category: 'reverse-engineering',
    difficulty: 'advanced',
    tags: ['cli', 'disassembler', 'debugger', 'framework', 'scriptable'],
    commands: [
      { command: 'r2 binary', description: 'Open a binary in radare2 interactive mode' },
      { command: 'r2 -d binary', description: 'Open in debug mode — set breakpoints, step through code, inspect registers' },
      { command: 'r2 -A binary', description: 'Open and auto-analyze — runs aaa automatically on load (saves time)' },
      { command: 'r2 -w binary', description: 'Open in write mode — allows patching/modifying the binary directly' },
      { command: 'aaa', description: 'Analyze all — functions, references, strings, signatures. Run this first after opening a binary' },
      { command: 'afl', description: 'List all detected functions with their address, size, and name' },
      { command: 'pdf @ main', description: 'Print disassembly of the main() function' },
      { command: 'pdf @ sym.check_password', description: 'Disassemble a specific named function' },
      { command: 'iz', description: 'List all printable strings found in the binary — look for passwords, URLs, keys' },
      { command: 'VV @ main', description: 'Enter visual graph mode for main() — shows control flow as a visual diagram' },
      { command: 'px 64 @ 0x08048000', description: 'Hexdump 64 bytes starting at address 0x08048000' },
      { command: '/R pop rdi', description: 'Search for ROP gadgets — essential for exploit development' },
    ],
    whenToUse: [
      'For fast CLI-based reverse engineering when you need speed over GUI comfort',
      'When scripting binary analysis pipelines (r2pipe works with Python, JS, Go, etc.)',
      'For CTF challenges where you need to quickly patch, debug, or analyze small binaries',
      'As a lightweight alternative to Ghidra/IDA when working over SSH on remote servers',
      'For ROP gadget finding during exploit development',
    ],
    commonFlags: [
      { flag: '-A', description: 'Auto-analyze binary on load (runs aaa)' },
      { flag: '-d', description: 'Open in debug mode' },
      { flag: '-w', description: 'Open in write mode (allows patching)' },
      { flag: '-z', description: 'Search for strings only' },
      { flag: '-q', description: 'Quiet mode — suppress banner' },
      { flag: '-c', description: 'Run r2 commands on launch (e.g., -c "aaa; afl")' },
    ],
    relatedTools: ['cutter', 'iaito', 'gdb', 'ghidra'],
    installation: 'sudo apt install radare2 -y   # or: git clone https://github.com/radareorg/radare2 && sys/install.sh',
    website: 'https://rada.re',
  },
  {
    id: 'gdb',
    name: 'GDB + GEF/PEDA',
    description: 'GNU Debugger — the standard Linux debugger enhanced with plugins like GEF or PEDA that add colored output, heap analysis, ROP gadget search, and exploit development helpers. Essential for buffer overflow exploitation and binary debugging.',
    category: 'reverse-engineering',
    difficulty: 'advanced',
    tags: ['debugger', 'exploit-dev', 'cli', 'buffer-overflow', 'must-know'],
    commands: [
      { command: 'gdb ./binary', description: 'Start debugging a binary' },
      { command: 'gdb -q ./binary', description: 'Start in quiet mode (skip banner)' },
      { command: 'run', description: 'Execute the program from the start' },
      { command: 'run $(python3 -c "print(\'A\'*200)")', description: 'Run with a long input to test for buffer overflows' },
      { command: 'break main', description: 'Set a breakpoint at main()' },
      { command: 'break *0x08048456', description: 'Set breakpoint at a specific memory address' },
      { command: 'ni / si', description: 'Step over / Step into the next instruction' },
      { command: 'info registers', description: 'Display all CPU register values — check EIP/RIP for control' },
      { command: 'x/20x $esp', description: 'Examine 20 hex words on the stack from ESP — see your buffer overflow payload' },
      { command: 'x/s 0x08048500', description: 'Print the string at a specific address' },
      { command: 'pattern create 200', description: '(GEF/PEDA) Generate a 200-byte cyclic pattern for finding exact overflow offset' },
      { command: 'pattern offset 0x41414141', description: '(GEF/PEDA) Calculate the exact offset where EIP was overwritten' },
      { command: 'checksec', description: '(GEF/PEDA) Check binary protections — NX, ASLR, PIE, Canary, RELRO' },
      { command: 'vmmap', description: '(GEF) Show virtual memory map — find writable/executable regions' },
      { command: 'ropper --search "pop rdi"', description: '(GEF) Search for ROP gadgets in the binary' },
    ],
    whenToUse: [
      'For exploit development — buffer overflows, ROP chains, format string attacks',
      'To dynamically debug Linux binaries and inspect runtime behavior',
      'For CTF binary exploitation (pwn) challenges',
      'When you need to trace program execution and find where crashes occur',
      'To verify exploit payloads hit the right memory addresses',
    ],
    commonFlags: [
      { flag: '-q', description: 'Quiet mode — skip startup messages' },
      { flag: '-x', description: 'Execute GDB commands from a file on startup' },
      { flag: '-p PID', description: 'Attach to a running process by PID' },
      { flag: '-ex "command"', description: 'Execute a specific command on startup' },
      { flag: '--args', description: 'Pass arguments to the debugged program' },
    ],
    relatedTools: ['lldb', 'edb-debugger', 'radare2', 'pwntools'],
    installation: 'sudo apt install gdb -y\nbash -c "$(curl -fsSL https://gef.blber.dev/sh)"   # Install GEF plugin',
    website: 'https://www.gnu.org/software/gdb',
  },
  {
    id: 'apktool',
    name: 'Apktool',
    description: 'Reverse engineering tool for Android APK files. Decodes resources to near-original form, allows modification, and rebuilds them back into a working APK. Essential for Android security testing — find hardcoded secrets, API keys, debug flags, and vulnerable configurations.',
    category: 'reverse-engineering',
    difficulty: 'intermediate',
    tags: ['android', 'apk', 'mobile', 'resources', 'smali'],
    commands: [
      { command: 'apktool d app.apk', description: 'Decode/decompile APK — extracts Smali code, resources, AndroidManifest.xml, and assets' },
      { command: 'apktool d -r app.apk', description: 'Decode without decoding resources — faster, use when you only care about code' },
      { command: 'apktool d -f -o output_dir app.apk', description: 'Force decode into a specific output directory (overwrite if exists)' },
      { command: 'apktool b target_dir -o modified.apk', description: 'Rebuild a modified APK from the decoded directory' },
      { command: 'grep -r "api_key\\|password\\|secret" target_dir/smali/', description: 'Search decoded Smali code for hardcoded secrets' },
      { command: 'cat target_dir/AndroidManifest.xml | grep "debuggable\\|allowBackup"', description: 'Check for dangerous manifest flags — debuggable=true is a critical finding' },
    ],
    whenToUse: [
      'To reverse engineer Android apps and find hardcoded API keys, tokens, and passwords',
      'To modify an APK (patch certificate pinning, enable debugging) and repackage it',
      'For analyzing Android malware by examining the decoded Smali bytecode',
      'To extract embedded URLs, endpoints, and backend server addresses from mobile apps',
    ],
    commonFlags: [
      { flag: 'd', description: 'Decode/decompile an APK file' },
      { flag: 'b', description: 'Build/recompile APK from decoded directory' },
      { flag: '-r', description: 'Do not decode resources (faster)' },
      { flag: '-f', description: 'Force overwrite output directory' },
      { flag: '-o', description: 'Specify output file/directory' },
    ],
    relatedTools: ['jadx', 'dex2jar', 'frida', 'objection'],
    installation: 'sudo apt install apktool -y',
    website: 'https://ibotpeaches.github.io/Apktool/',
  },
  {
    id: 'jadx',
    name: 'JADX',
    description: 'Dex-to-Java decompiler that produces readable Java source code from Android APK and DEX files. Features a powerful GUI with search, cross-references, and deobfuscation. Much easier to read than Smali output from Apktool.',
    category: 'reverse-engineering',
    difficulty: 'intermediate',
    tags: ['android', 'java', 'decompiler', 'gui'],
    commands: [
      { command: 'jadx-gui app.apk', description: 'Open APK in the GUI decompiler — browse classes, search strings, follow references visually' },
      { command: 'jadx -d out_dir app.apk', description: 'CLI decompile — outputs Java source files to a directory for grep/analysis' },
      { command: 'jadx --deobf app.apk -d out_dir', description: 'Decompile with deobfuscation — renames obfuscated classes/methods to readable names' },
      { command: 'jadx --show-bad-code app.apk -d out_dir', description: 'Force output even for code that fails to decompile cleanly — useful for complex apps' },
      { command: 'grep -r "http://\\|https://" out_dir/', description: 'After decompiling: find all hardcoded URLs and API endpoints' },
    ],
    whenToUse: [
      'When you need to read the actual Java logic of an Android app (not just Smali bytecode)',
      'For static analysis of Android app security — find auth bypasses, weak crypto, insecure storage',
      'To quickly search for strings like passwords, API keys, and hardcoded credentials in Java code',
      'When Apktool Smali output is too low-level — JADX gives you readable high-level Java code',
    ],
    commonFlags: [
      { flag: '-d', description: 'Output directory for decompiled source' },
      { flag: '--deobf', description: 'Enable deobfuscation of class/method names' },
      { flag: '--show-bad-code', description: 'Show decompilation errors inline instead of skipping' },
      { flag: '--no-res', description: 'Skip resource decompilation' },
      { flag: '-j', description: 'Number of processing threads' },
    ],
    relatedTools: ['apktool', 'dex2jar', 'jd-gui', 'bytecode-viewer'],
    installation: 'sudo apt install jadx -y',
    website: 'https://github.com/skylot/jadx',
  },
  {
    id: 'strace-ltrace',
    name: 'strace / ltrace',
    description: 'Linux tracing tools that intercept and log system calls (strace) and library calls (ltrace) made by a process. Reveals exactly what files a program opens, what network connections it makes, and what libraries it uses — essential for understanding unknown binaries.',
    category: 'reverse-engineering',
    difficulty: 'intermediate',
    tags: ['linux', 'tracing', 'syscalls', 'debugging', 'analysis'],
    commands: [
      { command: 'strace ./binary', description: 'Trace all system calls made by the binary — shows open(), read(), write(), connect(), etc.' },
      { command: 'strace -e trace=network ./binary', description: 'Only show network-related syscalls — find what connections the binary makes' },
      { command: 'strace -e trace=file ./binary', description: 'Only show file-related syscalls — see what files it reads/writes' },
      { command: 'strace -p PID', description: 'Attach to a running process and trace its syscalls live' },
      { command: 'strace -f ./binary', description: 'Follow child processes (trace forked processes too)' },
      { command: 'ltrace ./binary', description: 'Trace library calls — shows strcmp(), malloc(), printf() with arguments' },
      { command: 'ltrace -s 200 ./binary', description: 'Show up to 200 characters per string argument (default is 32)' },
    ],
    whenToUse: [
      'To understand what an unknown binary does without disassembling it — black-box analysis',
      'To find hardcoded passwords by watching strcmp()/strncmp() calls in ltrace output',
      'To identify malware behavior — what files it drops, what IPs it connects to',
      'For CTF challenges where the binary compares your input against a secret string',
      'To debug why a program fails — see which syscall returns an error',
    ],
    commonFlags: [
      { flag: '-e trace=', description: 'Filter by syscall category (file, network, process, memory)' },
      { flag: '-p', description: 'Attach to running process by PID' },
      { flag: '-f', description: 'Follow forked child processes' },
      { flag: '-o file', description: 'Write trace output to a file' },
      { flag: '-s N', description: 'Max string length to display (default: 32)' },
      { flag: '-c', description: 'Summary mode — count syscalls and show statistics' },
    ],
    relatedTools: ['gdb', 'radare2', 'procmon'],
    installation: 'sudo apt install strace ltrace -y',
    website: 'https://strace.io',
  },
];
