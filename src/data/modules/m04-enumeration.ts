import type { Module } from '../cehModules';

export const m04: Module = {
  id: 'm04',
  number: 'M04',
  title: 'Enumeration',
  description: 'Master advanced enumeration techniques to extract detailed environment signatures from target infrastructure. Enumeration is the most hands-on phase before exploitation — actively querying services to extract usernames, network shares, routing tables, SNMP data, LDAP directory trees, and service banners. Learn to leverage protocols like NetBIOS, SNMP, LDAP, NTP, SMTP, DNS, RPC, and NFS to build a complete target profile.',
  examWeight: '7%',
  estimatedQuestions: 9,
  duration: '4h 00m',
  topics: [
    {
      id: 'm04-t01',
      title: 'NetBIOS & SMB Enumeration',
      content: 'NetBIOS (Network Basic Input/Output System) operates on ports 137-139 and provides a critical attack surface on Windows networks. It exposes computer names, domain/workgroup membership, logged-in users, and shared resources. SMB (Server Message Block) on port 445 extends this further, allowing enumeration of shares, users, groups, password policies, and even SID (Security Identifier) brute-forcing. Null sessions — unauthenticated connections — are a classic attack vector that still works against misconfigured systems.',
      commands: [
        { command: 'nmap --script=nbstat.nse 192.168.1.100', description: 'Extract NetBIOS name table: computer name, domain, MAC address, and logged-in users' },
        { command: 'nbtscan 192.168.1.0/24', description: 'Rapid NetBIOS name scanning across an entire subnet to map Windows infrastructure' },
        { command: 'net view /domain', description: 'List all domains visible from the current Windows network context' },
        { command: 'net view \\\\192.168.1.100', description: 'List all shared resources on a specific Windows host' },
        { command: 'enum4linux -a 192.168.1.100', description: 'Exhaustive automated enumeration: users, shares, groups, password policy, OS info via SMB/RPC' },
        { command: 'smbclient -L //192.168.1.100 -N', description: 'List available SMB shares using a null session (no authentication)' },
        { command: 'smbmap -H 192.168.1.100', description: 'Enumerate SMB shares with permission levels (READ/WRITE/NO ACCESS)' },
        { command: 'smbmap -H 192.168.1.100 -u "" -p ""', description: 'SMB enumeration using explicit null session credentials' },
        { command: 'crackmapexec smb 192.168.1.100 --shares', description: 'CrackMapExec: enumerate shares with access level indicators' },
        { command: 'crackmapexec smb 192.168.1.100 --users', description: 'CrackMapExec: enumerate domain users via RID cycling' },
      ],
      keyPoints: [
        'NetBIOS ports: 137 (name service), 138 (datagram), 139 (session) — all prime enumeration targets',
        'SMB port: 445 (direct hosting) — the primary Windows file-sharing protocol and massive attack surface',
        'Null Session: Connecting to IPC$ share with empty credentials — classic technique to enumerate users and shares',
        'RID Cycling: Brute-forcing Windows Security Identifiers (SIDs) to enumerate user accounts even when null sessions are blocked',
        'NetBIOS Name Types: <00> Workstation, <20> File Server, <03> Messenger, <1D> Master Browser, <1B> Domain Master Browser',
        'SMBv1 is dangerous and should be disabled — it enables EternalBlue (MS17-010) and other critical exploits',
      ],
    },
    {
      id: 'm04-t02',
      title: 'SNMP Enumeration',
      content: 'Simple Network Management Protocol (SNMP) on UDP port 161/162 is designed for network device management but is a goldmine for attackers when misconfigured. Default community strings ("public" for read, "private" for read-write) are shockingly common. SNMP exposes the MIB (Management Information Base) tree which contains system descriptions, running processes, installed software, network interfaces, routing tables, ARP caches, and TCP connection states.',
      commands: [
        { command: 'snmpwalk -v2c -c public 192.168.1.100', description: 'Walk the entire MIB tree using SNMPv2c with the default "public" community string' },
        { command: 'snmpwalk -v2c -c public 192.168.1.100 1.3.6.1.2.1.25.4.2.1.2', description: 'Enumerate all running processes via the hrSWRunName OID' },
        { command: 'snmpwalk -v2c -c public 192.168.1.100 1.3.6.1.2.1.25.6.3.1.2', description: 'Enumerate all installed software packages via the hrSWInstalledName OID' },
        { command: 'snmpwalk -v2c -c public 192.168.1.100 1.3.6.1.4.1.77.1.2.25', description: 'Extract Windows user accounts via the LanManager MIB' },
        { command: 'onesixtyone -c /usr/share/seclists/Discovery/SNMP/common-snmp-community-strings.txt 192.168.1.100', description: 'Blazing fast SNMP community string brute-forcer' },
        { command: 'snmp-check 192.168.1.100', description: 'Automated SNMP enumeration: system info, users, processes, network interfaces, routing' },
        { command: 'nmap -sU -p 161 --script=snmp-sysdescr,snmp-processes,snmp-interfaces 192.168.1.100', description: 'Nmap NSE scripts for targeted SNMP enumeration' },
      ],
      keyPoints: [
        'SNMP Versions: v1 (cleartext, no auth), v2c (cleartext, bulk transfers), v3 (encrypted, authenticated) — only v3 is secure',
        'Community Strings: Act as passwords — "public" (read-only) and "private" (read-write) are devastating defaults',
        'MIB (Management Information Base): Hierarchical tree structure organizing all manageable device attributes via OIDs',
        'OID (Object Identifier): Numeric address within the MIB tree (e.g., 1.3.6.1.2.1.1.1 = sysDescr)',
        'SNMP Traps (port 162): Unsolicited alerts from devices — can be intercepted to gather intelligence passively',
        'Critical OIDs for CEH: sysDescr (1.3.6.1.2.1.1.1), sysName (1.3.6.1.2.1.1.5), interfaces (1.3.6.1.2.1.2), routing (1.3.6.1.2.1.4.21)',
        'AI-Enhanced SNMP: Modern tools use AI to automatically correlate OID data and identify anomalous configurations across large device inventories',
      ],
    },
    {
      id: 'm04-t03',
      title: 'LDAP & Active Directory Enumeration',
      content: 'LDAP (Lightweight Directory Access Protocol) on TCP port 389 (or 636 for LDAPS) is the backbone of Microsoft Active Directory. Unauthenticated or low-privilege LDAP queries can reveal the entire organizational structure: user accounts, groups, computer objects, group policy objects (GPOs), organizational units (OUs), and trust relationships. This information is the foundation for privilege escalation and lateral movement in enterprise environments.',
      commands: [
        { command: 'ldapsearch -x -H ldap://192.168.1.10 -b "dc=domain,dc=local"', description: 'Unauthenticated LDAP query to dump the entire directory tree' },
        { command: 'ldapsearch -x -H ldap://192.168.1.10 -s base namingcontexts', description: 'Discover the base DN (naming context) of the LDAP directory' },
        { command: 'ldapsearch -x -H ldap://192.168.1.10 -b "dc=domain,dc=local" "(objectClass=user)" cn sAMAccountName', description: 'Extract all user accounts with their common names and login usernames' },
        { command: 'ldapsearch -x -H ldap://192.168.1.10 -b "dc=domain,dc=local" "(objectClass=group)" cn member', description: 'Enumerate all groups and their members — critical for finding admin groups' },
        { command: 'nmap -p 389 --script ldap-search,ldap-rootdse 192.168.1.10', description: 'Nmap NSE LDAP enumeration: root DSE and directory search' },
        { command: 'ldapdomaindump -u "domain\\\\user" -p "password" ldap://192.168.1.10', description: 'Automated AD domain dump — exports users, groups, computers, and policies as HTML/JSON' },
      ],
      keyPoints: [
        'LDAP Ports: 389 (cleartext), 636 (LDAPS/TLS), 3268 (Global Catalog), 3269 (Global Catalog over TLS)',
        'Base DN (Distinguished Name): The root of the LDAP tree, typically "dc=domain,dc=com" — required for all queries',
        'Anonymous Bind: Many LDAP servers allow unauthenticated queries by default — a critical misconfiguration',
        'Key AD Attributes: sAMAccountName (login), memberOf (groups), userAccountControl (account flags), pwdLastSet (password age)',
        'userAccountControl flags: 0x0200 = Normal Account, 0x10000 = Password Never Expires, 0x400000 = Dont Require Pre-Auth (AS-REP roastable)',
        'BloodHound: Graphs AD relationships to automatically find attack paths to Domain Admin — the most powerful AD enumeration tool',
      ],
    },
    {
      id: 'm04-t04',
      title: 'NFS & RPC Enumeration',
      content: 'Network File System (NFS) on TCP port 2049 allows remote file system mounting. Misconfigured NFS exports can expose sensitive data, SSH keys, and even allow UID/GID spoofing for unauthorized access. RPC (Remote Procedure Call) on port 111 (portmapper) and 135 (Windows endpoint mapper) provides service registration that can be queried to discover available services and their ports.',
      commands: [
        { command: 'showmount -e 192.168.1.100', description: 'List all NFS exported directories and their access permissions' },
        { command: 'rpcinfo -p 192.168.1.100', description: 'Query the portmapper to list all registered RPC services and their ports' },
        { command: 'mount -t nfs 192.168.1.100:/share /mnt/target -o nolock', description: 'Mount an NFS share locally to browse and extract files' },
        { command: 'rpcclient -U "" -N 192.168.1.100', description: 'Connect to Windows RPC using null session for anonymous enumeration' },
        { command: 'rpcclient -U "" -N 192.168.1.100 -c "enumdomusers"', description: 'Enumerate all domain users via RPC null session' },
        { command: 'rpcclient -U "" -N 192.168.1.100 -c "enumdomgroups"', description: 'Enumerate all domain groups via RPC null session' },
        { command: 'rpcclient -U "" -N 192.168.1.100 -c "querydominfo"', description: 'Retrieve domain information: name, server role, total users, password policy' },
        { command: 'nmap -sV -p 111 --script=rpcinfo 192.168.1.100', description: 'Nmap NSE RPC enumeration to discover registered services' },
      ],
      keyPoints: [
        'NFS Port: 2049 — if exports are misconfigured with wildcards (*), anyone can mount and read/write files',
        'RPC Ports: 111 (Unix portmapper), 135 (Windows Endpoint Mapper) — gateway to discovering all RPC services',
        'rpcclient commands: enumdomusers, enumdomgroups, queryuser <RID>, netshareenum, getdompwinfo',
        'NFS UID Spoofing: If root_squash is disabled (no_root_squash), mounting with UID 0 gives root access to exports',
        'NFS Security: Kerberos authentication (sec=krb5) is the secure alternative to insecure host-based trust (sys)',
      ],
    },
    {
      id: 'm04-t05',
      title: 'SMTP & NTP Enumeration',
      content: 'SMTP (Simple Mail Transfer Protocol) on port 25 can reveal valid email addresses and usernames through the VRFY (verify user) and EXPN (expand mailing list) commands. NTP (Network Time Protocol) on UDP port 123 can reveal system information, connected clients, and internal IP addresses through the monlist and readvar commands. Both are frequently overlooked enumeration vectors.',
      commands: [
        { command: 'nmap -p 25 --script smtp-enum-users 192.168.1.100', description: 'Automated SMTP user enumeration via VRFY/EXPN/RCPT commands' },
        { command: 'smtp-user-enum -M VRFY -U users.txt -t 192.168.1.100', description: 'SMTP user enumeration using the VRFY method with a username wordlist' },
        { command: 'smtp-user-enum -M RCPT -U users.txt -t 192.168.1.100', description: 'SMTP enumeration via RCPT TO — often works when VRFY is disabled' },
        { command: 'ntpq -c readvar 192.168.1.100', description: 'Query NTP server variables: system description, OS, and version' },
        { command: 'ntpq -c peers 192.168.1.100', description: 'List NTP peers and their synchronization status — reveals network topology' },
        { command: 'nmap -sU -p 123 --script=ntp-info,ntp-monlist 192.168.1.100', description: 'NTP enumeration: monlist reveals up to 600 recently connected clients' },
      ],
      keyPoints: [
        'SMTP VRFY: Confirms if a username exists on the mail server — "252 User exists" vs "550 No such user"',
        'SMTP EXPN: Expands a mailing list to reveal all member email addresses',
        'SMTP RCPT TO: If VRFY is disabled, RCPT TO can be used to verify email addresses indirectly',
        'NTP monlist: Returns a list of up to 600 hosts that recently communicated with the NTP server — reveals internal IPs',
        'NTP stratum: Indicates distance from reference clock (stratum 1 = atomic clock, stratum 2 = syncs from stratum 1)',
        'NTP Amplification: The monlist command can be abused for DDoS amplification (response 200x larger than request)',
      ],
    },
    {
      id: 'm04-t06',
      title: 'DNS Enumeration & Zone Transfers',
      content: 'DNS enumeration in the context of this module goes deeper than footprinting — it involves active brute-forcing of subdomains, SRV record discovery for service mapping, reverse DNS sweeps to map IP ranges, and attempts to exploit misconfigured zone transfers. DNS enumeration is particularly valuable for discovering internal naming conventions, development/staging environments, and shadow IT.',
      commands: [
        { command: 'dnsenum --enum example.com', description: 'Comprehensive DNS enumeration: brute-force subdomains, zone transfers, reverse lookups' },
        { command: 'fierce --domain example.com', description: 'Aggressive DNS reconnaissance: zone transfers, wildcard detection, nearby IP scanning' },
        { command: 'dnsrecon -d example.com -t axfr', description: 'Attempt zone transfers against all discovered nameservers' },
        { command: 'dnsrecon -d example.com -t brt -D /usr/share/seclists/Discovery/DNS/subdomains-top1million-5000.txt', description: 'DNS brute-force using SecLists wordlist for deep subdomain discovery' },
        { command: 'dig SRV _sip._tcp.example.com', description: 'Enumerate SRV records to discover specific services (SIP, LDAP, Kerberos)' },
        { command: 'dnsrecon -r 192.168.1.0/24', description: 'Reverse DNS sweep — map IP addresses to hostnames across an entire subnet' },
      ],
      keyPoints: [
        'SRV Records: _ldap._tcp, _kerberos._tcp, _sip._tcp — reveal exact service locations and ports',
        'DNS Brute-forcing: Systematically guessing subdomains (dev., staging., admin., vpn., mail.) using wordlists',
        'Reverse DNS Sweep: Querying PTR records for every IP in a range to discover hidden hostnames',
        'DNSSEC: DNS Security Extensions — prevents DNS spoofing but can leak zone information through NSEC walking',
        'Wildcard Detection: If *.domain.com resolves, DNS brute-forcing will produce false positives — tools must handle this',
      ],
    },
    {
      id: 'm04-t07',
      title: 'IPsec, VoIP & BGP Enumeration',
      content: 'Specialized protocols present unique enumeration opportunities. IPsec VPN gateways can be fingerprinted and their configurations enumerated using IKE scanning. VoIP (SIP/H.323) infrastructure exposes extensions, user agents, and call routing. BGP routers can reveal autonomous system relationships and network topology through publicly accessible looking glass servers.',
      commands: [
        { command: 'ike-scan -M 192.168.1.1', description: 'Fingerprint IPsec VPN gateways: vendor, encryption algorithms, authentication methods' },
        { command: 'svmap 192.168.1.0/24', description: 'SIP scanner: discover VoIP devices and PBX systems on the network' },
        { command: 'svwar -e100-200 192.168.1.100', description: 'SIP extension enumeration: brute-force valid phone extensions (100-200)' },
        { command: 'nmap --script=sip-enum-users -p 5060 192.168.1.100', description: 'Nmap SIP user enumeration via OPTIONS/REGISTER methods' },
      ],
      keyPoints: [
        'IKE (Internet Key Exchange): Port 500 — Phase 1 negotiation reveals VPN vendor, encryption suite, and authentication mode',
        'SIP (Session Initiation Protocol): Port 5060/5061 — VoIP signaling protocol vulnerable to extension enumeration and toll fraud',
        'H.323: Legacy VoIP protocol on port 1720 — often found in older enterprise environments',
        'BGP Looking Glass: Publicly accessible BGP routers that reveal routing tables and AS path relationships',
        'Enumeration vs Scanning: Enumeration is ACTIVE and creates log entries — always have authorization',
      ],
    },
    {
      id: 'm04-t08',
      title: 'AI-Powered Enumeration & Automation',
      content: 'Modern penetration testing increasingly leverages AI and machine learning to accelerate enumeration. AI-powered tools can automatically correlate data from multiple enumeration sources, identify patterns in usernames and passwords, predict valid credentials, and prioritize high-value targets. Tools like ShellGPT, AutoRecon, and custom scripts using OpenAI APIs can dramatically reduce the manual effort required for comprehensive enumeration.',
      commands: [
        { command: 'autorecon 192.168.1.100', description: 'AutoRecon: Fully automated multi-threaded enumeration — runs Nmap, Gobuster, Nikto, enum4linux simultaneously' },
        { command: 'sgpt "generate an enum4linux command to enumerate users on 192.168.1.100"', description: 'ShellGPT: AI-assisted command generation for enumeration tasks' },
      ],
      keyPoints: [
        'AutoRecon: Automates the entire enumeration workflow — runs service-specific enumeration scripts based on Nmap results',
        'AI Username Prediction: ML models can predict valid usernames based on discovered naming conventions (first.last, flast, etc.)',
        'Data Correlation: AI tools cross-reference LDAP users, SMTP enumeration, and SMB data to build comprehensive user profiles',
        'CEH v13 AI Focus: The exam tests awareness of AI-powered tools for both attack and defense — know ShellGPT and AI-based automation',
        'Automated Reporting: AI can generate structured enumeration reports from raw tool output, identifying critical findings automatically',
      ],
    },
  ],
  keyTools: ['enum4linux', 'snmpwalk', 'ldapsearch', 'smbmap', 'dnsenum', 'fierce', 'rpcclient', 'crackmapexec', 'AutoRecon', 'BloodHound'],
  countermeasures: [
    'Use SNMPv3 with authentication and encryption — never use default community strings (public/private)',
    'Disable unnecessary services: NetBIOS over TCP/IP, NFS with wildcard exports, SMTP VRFY/EXPN commands',
    'Implement strict LDAP access controls: require authentication, use LDAPS (port 636), limit anonymous binds',
    'Restrict SMB null sessions via Group Policy: set RestrictAnonymous and RestrictNullSessAccess',
    'Configure NFS with root_squash, restrict exports to specific IP ranges, use Kerberos authentication',
    'Monitor for enumeration attempts: failed login spikes, LDAP query floods, SNMP brute-force attempts',
    'Disable NTP monlist to prevent information disclosure and DDoS amplification',
    'Implement network segmentation to limit lateral enumeration between VLANs',
  ],
  commonPorts: [
    { port: 137, protocol: 'UDP', service: 'NetBIOS Name', description: 'NetBIOS Name Service — resolves NetBIOS names to IP addresses' },
    { port: 138, protocol: 'UDP', service: 'NetBIOS Datagram', description: 'NetBIOS Datagram Service — connectionless messaging' },
    { port: 139, protocol: 'TCP', service: 'NetBIOS Session', description: 'NetBIOS Session Service — connection-oriented file/print sharing' },
    { port: 161, protocol: 'UDP', service: 'SNMP', description: 'Simple Network Management Protocol — device management and monitoring' },
    { port: 162, protocol: 'UDP', service: 'SNMP Trap', description: 'SNMP Trap — unsolicited alerts from managed devices' },
    { port: 389, protocol: 'TCP', service: 'LDAP', description: 'Lightweight Directory Access Protocol — Active Directory queries' },
    { port: 445, protocol: 'TCP', service: 'SMB', description: 'Server Message Block — Windows file/print sharing (direct hosting)' },
    { port: 636, protocol: 'TCP', service: 'LDAPS', description: 'LDAP over TLS — encrypted directory access' },
    { port: 2049, protocol: 'TCP', service: 'NFS', description: 'Network File System — Unix/Linux remote file sharing' },
    { port: 111, protocol: 'TCP', service: 'RPCBind', description: 'RPC Portmapper — maps RPC services to ports' },
    { port: 135, protocol: 'TCP', service: 'MSRPC', description: 'Microsoft RPC Endpoint Mapper — Windows service discovery' },
  ],
  examTips: [
    'Know the difference between enumeration and scanning — enumeration extracts specific data (usernames, shares), scanning finds open ports',
    'SNMP community strings: "public" = read-only, "private" = read-write. If exam mentions "community string = public", they can READ device info',
    'Null sessions use IPC$ share with empty username/password — this is the classic Windows enumeration technique',
    'enum4linux is THE go-to tool for SMB enumeration on the exam — know it combines multiple techniques in one tool',
    'LDAP anonymous bind = no credentials needed = full directory access. This is a critical misconfiguration tested on the exam',
    'SNMP OIDs: You do NOT need to memorize OID numbers, but understand what types of information SNMP reveals (processes, users, interfaces)',
    'NFS showmount -e reveals exports — if configured with *, anyone can mount the share. This is heavily tested.',
    'RPC enumeration via rpcclient is used to enumerate users and groups when SMB null sessions fail',
    'Know that enumeration is ALWAYS active and creates logs — it requires explicit authorization',
  ],
  realWorldScenarios: [
    'During an internal penetration test, you discover SNMP running on network switches with the default "public" community string. Using snmpwalk, you extract the entire routing table, ARP cache, and VLAN configurations — mapping the network without a single port scan.',
    'You attempt a null session on a Windows 2016 server but it fails due to RestrictAnonymous being set. You pivot to LDAP anonymous bind on port 389, which is still enabled, and dump 2,300 user accounts with their group memberships.',
    'An NFS server exports /home with no_root_squash and wildcard (*) access. You mount the share, navigate to the admin user\'s .ssh directory, and extract their private SSH key — gaining root access to the server without any exploitation.',
    'Using AutoRecon, you automate the enumeration of 50 hosts in parallel. The tool discovers SMB shares, SNMP community strings, NFS exports, and LDAP data simultaneously, generating a comprehensive report in under 10 minutes.',
  ],
  prerequisites: ['M03 — Scanning identifies open ports that enumeration then interrogates in depth'],
};
