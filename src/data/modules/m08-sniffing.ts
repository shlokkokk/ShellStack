import type { Module } from '../cehModules';

export const m08: Module = {
  id: 'm08',
  number: 'M08',
  title: 'Sniffing',
  description: 'Gain deep visibility into network traffic by compromising OSI Layer 2 and Layer 3 infrastructure. Master techniques to bypass switched networks using MAC flooding, ARP poisoning, and DHCP starvation. Learn to intercept cleartext protocols, execute complex Man-in-the-Middle (MitM) attacks with SSL Stripping, extract credentials from the wire, and perform advanced PCAP forensic analysis using Wireshark and tshark.',
  examWeight: '5%',
  estimatedQuestions: 6,
  duration: '3h 00m',
  topics: [
    {
      id: 'm08-t01',
      title: 'Sniffing Fundamentals & Hardware',
      content: 'Packet sniffing is the process of intercepting and logging traffic passing over a digital network. In modern switched networks, switches only forward traffic to the specific physical port where the destination MAC address resides. Therefore, a standard sniffer only sees broadcast traffic, multicast traffic, and its own traffic. Attackers must employ active Layer 2 manipulation techniques to force the switch to send other users\' traffic to their port.',
      keyPoints: [
        'Passive Sniffing: Works only on hubs (legacy) or specific wireless setups where all traffic is broadcast to all ports. Extremely difficult to detect because no packets are injected.',
        'Active Sniffing: Required on modern switched networks. Involves actively injecting traffic (e.g., ARP spoofing, MAC flooding) to intercept packets. Highly detectable by Intrusion Detection Systems (IDS).',
        'Promiscuous Mode: Configuring the network interface card (NIC) to process all packets it sees, regardless of the destination MAC address. Required for software sniffing.',
        'Hardware Sniffers / Network Taps: Physical devices placed inline between network nodes. They are completely undetectable by software because they operate silently at the physical layer (Layer 1).',
        'SPAN/Port Mirroring: A switch configuration that copies traffic from one port to another — often abused by attackers who compromise the management interface of the switch.',
      ],
    },
    {
      id: 'm08-t02',
      title: 'MAC Flooding & Switch Compromise',
      content: 'A switch uses a CAM (Content Addressable Memory) table to map MAC addresses to physical ports. If an attacker floods the switch with thousands of fake, randomly generated MAC addresses, the CAM table fills up instantly. Many switches fail open, entering a "hub-like" failopen state where they broadcast all incoming traffic to all ports to ensure connectivity, allowing the attacker to sniff the entire network segment.',
      commands: [
        { command: 'macof -i eth0', description: 'Classic Dsniff tool: Flood the network with random MAC addresses to overflow the switch CAM table' },
        { command: 'macof -i eth0 -n 100000', description: 'Send exactly 100,000 random MAC addresses to overwhelm the switch memory' },
      ],
      keyPoints: [
        'CAM Table (MAC Table): Stores MAC-to-Port mappings. Has a limited size (e.g., 8192 or 16384 entries depending on the switch model).',
        'Failopen Behavior: When the CAM table is full, the switch cannot learn new addresses and reverts to broadcasting traffic to all ports (acting like a hub).',
        'Switch MAC Spoofing: An attacker changes their MAC address to match the target\'s MAC address. The switch updates its CAM table, and traffic intended for the target is sent to the attacker.',
        'Countermeasure - Port Security: Configure Cisco switches to allow only a specific number of MAC addresses per port (e.g., `switchport port-security maximum 1`). If exceeded, the port shuts down (err-disable).',
      ],
    },
    {
      id: 'm08-t03',
      title: 'ARP Poisoning (Man-in-the-Middle)',
      content: 'Address Resolution Protocol (ARP) maps IP addresses to MAC addresses on a local subnet. ARP is stateless and fundamentally insecure: it trusts all replies, even if no request was ever sent. In an ARP Poisoning attack, the attacker sends fake (gratuitous) ARP replies to a target (e.g., victim PC) and the gateway (e.g., router), claiming to be the other party. All traffic between them then flows through the attacker.',
      commands: [
        { command: 'echo 1 > /proc/sys/net/ipv4/ip_forward', description: 'CRITICAL: Enable IP forwarding in Linux so intercepted traffic is passed along to the real destination, preventing a total Denial of Service' },
        { command: 'arpspoof -i eth0 -t 192.168.1.100 192.168.1.1', description: 'Tell the victim (100) that your MAC address is the router (1)' },
        { command: 'arpspoof -i eth0 -t 192.168.1.1 192.168.1.100', description: 'Tell the router (1) that your MAC address is the victim (100) — completes the full two-way MitM' },
        { command: 'ettercap -T -q -i eth0 -M arp:remote /192.168.1.100// /192.168.1.1//', description: 'Ettercap text-mode ARP poisoning MitM attack' },
        { command: 'bettercap -iface eth0', description: 'Launch Bettercap, the modern, modular framework for network attacks and MitM' },
        { command: 'bettercap -eval "arp.spoof on; net.sniff on"', description: 'Automated Bettercap ARP spoofing and packet sniffing' },
      ],
      keyPoints: [
        'Gratuitous ARP: An ARP reply sent without a request — often abused to update target ARP caches maliciously.',
        'IP Forwarding: Must be enabled before ARP poisoning, otherwise the attacker drops the intercepted packets, causing a DoS rather than a stealthy MitM.',
        'Scope constraint: ARP poisoning ONLY works on the local subnet (Layer 2). You cannot ARP spoof across routers to the internet.',
        'Tools: arpspoof, Ettercap, Bettercap, Cain & Abel (legacy Windows but still tested).',
        'Countermeasure - Dynamic ARP Inspection (DAI): Switch feature that validates ARP packets against the DHCP snooping binding database, dropping malicious spoofed packets.',
      ],
    },
    {
      id: 'm08-t04',
      title: 'DHCP Starvation & DNS Spoofing',
      content: 'Beyond ARP, attackers can manipulate other critical network protocols to achieve MitM. DHCP starvation involves sending thousands of DHCP requests with spoofed MAC addresses to exhaust the legitimate DHCP server\'s IP pool. Once starved, the attacker sets up a Rogue DHCP Server to hand out malicious IP configurations (especially setting themselves as the default gateway). DNS spoofing intercepts UDP port 53 requests and returns the attacker\'s IP, redirecting the victim to phishing sites.',
      commands: [
        { command: 'dhcpstarv -i eth0', description: 'Exhaust the DHCP server\'s IP address pool by sending rapid, spoofed requests' },
        { command: 'yersinia -G', description: 'Launch Yersinia GUI for Layer 2 attacks (DHCP starvation, STP manipulation, CDP attacks)' },
        { command: 'dnsspoof -i eth0 -f hosts.txt', description: 'Spoof DNS responses based on entries in a local hosts file (requires MitM to intercept the requests first)' },
        { command: 'bettercap -eval "set dns.spoof.domains *; dns.spoof on"', description: 'Bettercap module to spoof all DNS requests and redirect them to the attacker IP' },
      ],
      keyPoints: [
        'Rogue DHCP Server: Once the legitimate server is starved, the attacker\'s rogue server offers IPs with the attacker set as the Default Gateway and malicious DNS servers.',
        'DNS Spoofing/Cache Poisoning: Intercepting UDP port 53 traffic and answering before the real DNS server. Often combined with ARP poisoning to intercept the traffic locally.',
        'Countermeasure - DHCP Snooping: Switch feature that blocks DHCP offers from untrusted ports (only allowing them from the port connected to the legitimate server).',
      ],
    },
    {
      id: 'm08-t05',
      title: 'SSL Stripping & Advanced MitM',
      content: 'Modern networks heavily use HTTPS (TLS/SSL) to encrypt traffic, rendering basic sniffing useless for credential extraction. SSL Stripping (HTTPS Downgrade) attacks exploit the fact that users rarely type "https://". The attacker intercepts the initial HTTP request, establishes a secure HTTPS connection with the real server, but serves an unencrypted HTTP connection back to the victim, capturing all credentials in cleartext.',
      commands: [
        { command: 'sslstrip -l 8080', description: 'Run SSLStrip on port 8080 (requires iptables to redirect port 80 traffic to 8080)' },
        { command: 'iptables -t nat -A PREROUTING -p tcp --destination-port 80 -j REDIRECT --to-port 8080', description: 'Redirect HTTP traffic to the SSLStrip listener' },
        { command: 'bettercap -eval "set https.proxy.sslstrip true; https.proxy on"', description: 'Modern, automated SSL stripping attack using Bettercap' },
      ],
      keyPoints: [
        'SSL Stripping Mechanism: Attacker maintains HTTPS with the server, but strips the "S" and serves HTTP to the victim. The padlock icon disappears in the victim\'s browser.',
        'HSTS (HTTP Strict Transport Security): A server directive telling the browser NEVER to connect via HTTP. HSTS completely defeats SSL Stripping, provided the user has visited the site securely at least once before.',
        'Certificate Spoofing: Presenting a fake SSL certificate. Causes a massive browser warning ("Your connection is not private") which most users ignore.',
      ],
    },
    {
      id: 'm08-t06',
      title: 'Packet Analysis & Forensics',
      content: 'Once traffic is intercepted, the attacker must extract valuable data. Cleartext protocols (HTTP, FTP, Telnet) immediately reveal credentials. Even encrypted traffic can yield metadata (SNI headers, packet sizes). Wireshark and tcpdump are the industry standards for packet analysis, utilizing powerful display filters to sift through gigabytes of noise.',
      commands: [
        { command: 'tcpdump -i eth0 -w capture.pcap', description: 'Capture all traffic on eth0 and write it to a PCAP file for offline analysis' },
        { command: 'tcpdump -i eth0 port 80 or port 21', description: 'Capture only HTTP and FTP traffic' },
        { command: 'tshark -r capture.pcap -Y "http.request.method == POST"', description: 'Use terminal Wireshark (tshark) to extract HTTP POST requests (which often contain login forms)' },
        { command: 'tshark -r capture.pcap -T fields -e http.authbasic', description: 'Extract Base64 encoded Basic Authentication credentials directly from a PCAP' },
      ],
      keyPoints: [
        'Cleartext Protocols: FTP (21), Telnet (23), SMTP (25), HTTP (80), POP3 (110), IMAP (143), SNMPv1/v2 (161). These are primary targets for sniffing.',
        'Wireshark Display Filters: e.g., `ip.addr == 192.168.1.100`, `tcp.port == 80`, `http.request.method == "POST"`, `ftp.request.command == "PASS"`.',
        'Follow TCP Stream: Wireshark feature to view the entire back-and-forth payload of a conversation in human-readable format, instantly revealing cleartext credentials.',
        'PCAP (Packet Capture): The standard file format for saving network traffic. Crucial for both attackers (credential hunting) and defenders (incident response forensics).',
      ],
    },
  ],
  keyTools: ['Wireshark', 'tcpdump', 'Bettercap', 'Ettercap', 'macof', 'arpspoof', 'Yersinia', 'SSLstrip'],
  countermeasures: [
    'Implement Port Security on switches to prevent MAC flooding by restricting the number of MAC addresses per port.',
    'Enable Dynamic ARP Inspection (DAI) on switches to block spoofed ARP packets.',
    'Enable DHCP Snooping to prevent rogue DHCP servers and starvation attacks.',
    'Use encrypted protocols exclusively: SSH instead of Telnet, SFTP/SCP instead of FTP, HTTPS instead of HTTP, SNMPv3 instead of v1/v2.',
    'Implement HSTS (HTTP Strict Transport Security) on all web servers to defeat SSL Stripping attacks.',
    'Use VPNs, IPsec, or zero-trust network access (ZTNA) to encrypt traffic end-to-end within the local network.',
    'Implement 802.1X Port-Based Network Access Control to prevent unauthorized devices from connecting to switch ports in the first place.',
  ],
  examTips: [
    'Passive sniffing = Hubs. Active sniffing = Switches (requires ARP poisoning or MAC flooding to manipulate the CAM table).',
    'MAC Flooding fills the CAM table. The tool is macof. The countermeasure is Port Security.',
    'ARP Poisoning requires IP Forwarding to be enabled, otherwise it causes a Denial of Service. It ONLY works on the local subnet (Layer 2).',
    'DHCP Starvation exhausts the IP pool. The countermeasure is DHCP Snooping.',
    'Know the cleartext protocols: FTP, Telnet, POP3, IMAP, HTTP. These are always the targets in sniffing questions.',
    'A Hardware Protocol Analyzer or Network Tap operates at the physical layer and is completely undetectable by the target OS.',
    'If a question asks how to intercept HTTPS traffic without causing browser warnings (assuming no HSTS), the answer is SSL Stripping.',
  ],
  realWorldScenarios: [
    'During an internal pentest, you plug into an open wall jack in a conference room. You run `arpspoof` against the default gateway and a target database administrator\'s PC. You capture their cleartext SQL authentication via Wireshark, gaining SA privileges to the backend database.',
    'You are assessing a legacy network without port security. You use `macof` to flood the switch. Within seconds, the switch fails open, and your Wireshark capture fills with traffic from all other ports, entirely bypassing the VLAN segregation.',
    'You use Bettercap to perform an ARP spoofing attack combined with the `https.proxy` module. When a user types `bank.com`, Bettercap intercepts the HTTP redirect, establishes its own HTTPS connection to the bank, but serves unencrypted HTTP to the user (SSL Stripping), capturing their login credentials in cleartext.',
    'As an incident responder, you are handed a 5GB PCAP file. Instead of opening it in Wireshark, you use `tshark` with a display filter (`tshark -r capture.pcap -Y "http.request.method == POST"`) to instantly extract the exact moment the attacker exfiltrated data via an HTTP POST request.',
  ],
  prerequisites: ['M03 — Understanding TCP/IP fundamentals is required before manipulating network traffic at Layers 2 and 3.'],
};
