import type { Module } from '../cehModules';

export const m03: Module = {
  id: 'm03',
  number: 'M03',
  title: 'Scanning Networks',
  description: 'Advanced network scanning techniques, stealth port scanning, OS fingerprinting, and IDS/Firewall evasion. Master the TCP/IP protocol suite to craft packets, analyze responses, and build a complete picture of the target network architecture.',
  examWeight: '8%',
  estimatedQuestions: 10,
  duration: '3h 45m',
  topics: [
    {
      id: 'm03-t01',
      title: 'Host Discovery & Ping Sweeps',
      content: 'Before bombarding ports, attackers must determine which IP addresses are actually live. Host discovery generally uses ICMP, ARP (on local subnets), and TCP/UDP ping sweeps to map active machines without raising loud IDS alarms for full port scans. Understanding how to ping sweep covertly is crucial for mapping massive subnets rapidly.',
      commands: [
        { command: 'nmap -sn -PE 192.168.1.0/24', description: 'Ping sweep using standard ICMP Echo Requests' },
        { command: 'nmap -PR 192.168.1.0/24', description: 'Unblockable ARP ping scan for local network discovery' },
        { command: 'masscan 10.0.0.0/8 -p80 --rate 10000', description: 'Masscan asynchronous high-speed host discovery via port 80' },
        { command: 'nmap -sn -PA21,22,25,3389 192.168.1.0/24', description: 'TCP ACK ping sweep bypassing firewalls that block ICMP' },
        { command: 'nmap -sn -PS22,80,443 192.168.1.0/24', description: 'TCP SYN ping sweep targeting common service ports' },
        { command: 'nmap -Pn 192.168.1.1', description: 'Skip host discovery entirely — treat all hosts as online, bypass ping-blocking firewalls' },
      ],
      keyPoints: [
        'ARP Ping (-PR): Works only on local subnets but is impossible to block — the most reliable discovery method on LAN',
        'ICMP Echo (-PE): Standard ping — easily blocked by firewalls but simple and fast',
        'TCP SYN Ping (-PS): Sends SYN to specified ports — open port replies SYN-ACK, closed replies RST, both confirm host is live',
        'TCP ACK Ping (-PA): Sends ACK — always gets RST from live hosts, effective at bypassing stateless firewalls',
        'UDP Ping (-PU): Sends UDP to typically closed ports — ICMP Unreachable confirms host is alive',
      ],
    },
    {
      id: 'm03-t02',
      title: 'Deep Port Scanning & TCP Handshake Analysis',
      content: 'Port scanning involves sending specially crafted TCP, UDP, or SCTP packets and analyzing the response flags (SYN/ACK/RST) to determine if a service is listening. Understanding the 3-way TCP handshake is mandatory. The stealth SYN scan is the industry standard, bypassing primitive loggers by never completing the full TCP connection.',
      commands: [
        { command: 'nmap -sT 192.168.1.1', description: 'TCP Connect scan (Loud, completes full 3-way handshake, easily logged)' },
        { command: 'nmap -sS -p- 192.168.1.1', description: 'SYN "Stealth" scan across all 65535 ports — half-open, never completes handshake' },
        { command: 'nmap -sU --top-ports 100 192.168.1.1', description: 'UDP scan for stateless protocols like DNS (53), SNMP (161), TFTP (69)' },
        { command: 'nmap -sX 192.168.1.1', description: 'XMAS scan — sets FIN, URG, and PUSH flags to bypass stateless firewalls (RFC 793 compliant only)' },
        { command: 'nmap -sN 192.168.1.1', description: 'NULL scan — sends no flags. Open/filtered ports give no response, closed ports send RST' },
        { command: 'nmap -sA 192.168.1.1', description: 'ACK scan — does not find open ports, but maps firewall rules (filtered vs unfiltered)' },
        { command: 'nmap -sW 192.168.1.1', description: 'Window scan — like ACK scan but examines TCP window size to differentiate open vs closed ports' },
      ],
      keyPoints: [
        'TCP 3-Way Handshake: SYN → SYN-ACK → ACK. This establishes a connection.',
        'SYN Scan (-sS): Sends SYN, receives SYN-ACK (open) or RST (closed). Sends RST instead of ACK — connection never completes.',
        'Connect Scan (-sT): Full handshake. Reliable but loud — creates log entries on the target.',
        'XMAS/NULL/FIN Scans: Exploit RFC 793 — these scans do NOT work against Windows systems (non-RFC compliant).',
        'ACK Scan (-sA): Purpose is firewall mapping, not port discovery. Shows which ports are filtered vs unfiltered.',
        'Port States: open, closed, filtered (firewall dropping), unfiltered (ACK scan), open|filtered (no response)',
      ],
    },
    {
      id: 'm03-t03',
      title: 'Service Profiling & OS Fingerprinting',
      content: 'Discovering an open port is useless without knowing exactly what is running behind it. Attackers heavily rely on service version detection (probing the service to leak its banner and version) and OS fingerprinting (analyzing the exact structure of TCP/IP parameters like TTL and Window Size to guess the underlying kernel).',
      commands: [
        { command: 'nmap -sV --version-intensity 9 192.168.1.1', description: 'Aggressive service version detection and banner grabbing' },
        { command: 'nmap -O --osscan-guess 192.168.1.1', description: 'Aggressive Operating System kernel fingerprinting with best guesses' },
        { command: 'nmap -A -T4 192.168.1.1', description: 'Enable OS detection, version detection, script scanning, and traceroute simultaneously' },
        { command: 'nmap -sS -sV -sC -O -p- -T4 192.168.1.1 -oA full_scan', description: 'Ultimate comprehensive scan saving results in all formats' },
      ],
      keyPoints: [
        'Banner Grabbing: Connecting to a service and reading its welcome message to identify software and version',
        'OS Fingerprinting uses TCP/IP stack characteristics: TTL (64=Linux, 128=Windows, 255=Cisco), Window Size, DF bit',
        'Active OS Fingerprinting (-O): Sends crafted probes and analyzes responses — requires at least 1 open and 1 closed port',
        'Passive OS Fingerprinting: Tools like p0f analyze traffic without sending packets — completely undetectable',
        '-sC flag: Runs default NSE scripts — equivalent to --script=default',
      ],
    },
    {
      id: 'm03-t04',
      title: 'IDS & Firewall Evasion',
      content: 'Modern targets are protected by Intrusion Detection Systems (IDS) and stateful firewalls. To bypass them, traffic must be manipulated. This includes fragmenting packets to break signatures, using decoy IP addresses to obscure the origin, spoofing MAC/IPs, and altering packet timing to stay below threshold alerts.',
      commands: [
        { command: 'nmap -f --mtu 24 192.168.1.1', description: 'Fragment packets into 24-byte chunks to bypass basic IDS signature matching' },
        { command: 'nmap -D RND:10 192.168.1.1', description: 'Hide the scan by generating traffic from 10 random Decoy IPs simultaneously' },
        { command: 'nmap -S 192.168.1.254 -e eth0 192.168.1.1', description: 'Spoof the source IP address as the network gateway to evade ACLs' },
        { command: 'nmap -T1 192.168.1.1', description: 'Sneaky timing profile to evade rate-based security sensors' },
        { command: 'nmap --source-port 53 192.168.1.1', description: 'Spoof source port as DNS (53) — many firewalls allow DNS traffic inbound' },
        { command: 'nmap --data-length 25 192.168.1.1', description: 'Append random data to evade packet size-based IDS signatures' },
        { command: 'nmap --badsum 192.168.1.1', description: 'Send packets with invalid checksums — firewalls/IDS may pass them, but real hosts drop them' },
      ],
      keyPoints: [
        'Packet Fragmentation (-f): Splits the IP header across multiple packets — basic IDS may fail to reassemble',
        'Decoys (-D): Mix your real scan traffic with spoofed source IPs — makes attribution nearly impossible',
        'Source Port Spoofing (--source-port): Port 53 (DNS) or 80 (HTTP) often bypass firewall rules',
        'Timing (-T0 to -T5): Lower values = slower scan = less likely to trigger rate-based alerts',
        'Idle Scan (-sI): The ultimate stealth scan — uses a "zombie" host to scan the target. Your IP never appears in target logs.',
      ],
    },
    {
      id: 'm03-t05',
      title: 'Nmap Output & Scripting Engine',
      content: 'Nmap supports multiple output formats for integration with other tools and reporting. The Nmap Scripting Engine (NSE) extends Nmap from a port scanner into a full vulnerability assessment platform with hundreds of scripts for service enumeration, vulnerability detection, and even exploitation.',
      commands: [
        { command: 'nmap -oN scan.txt 192.168.1.1', description: 'Save results in normal (human-readable) format' },
        { command: 'nmap -oX scan.xml 192.168.1.1', description: 'Save results in XML format for tool integration' },
        { command: 'nmap -oG scan.gnmap 192.168.1.1', description: 'Save results in grepable format for scripting' },
        { command: 'nmap -oA scan_all 192.168.1.1', description: 'Save in ALL formats simultaneously (normal, XML, grepable)' },
        { command: 'nmap --script vuln 192.168.1.1', description: 'Run all vulnerability detection NSE scripts against the target' },
        { command: 'nmap --script=http-enum 192.168.1.1', description: 'Enumerate web directories, admin panels, and common files' },
        { command: 'nmap --script "smb-vuln-*" -p 445 192.168.1.1', description: 'Check for all known SMB vulnerabilities including EternalBlue' },
      ],
      keyPoints: [
        'NSE Script Categories: auth, broadcast, brute, default, discovery, dos, exploit, external, fuzzer, intrusive, malware, safe, version, vuln',
        '-sC is equivalent to --script=default — runs safe, non-intrusive scripts automatically',
        'NSE scripts location: /usr/share/nmap/scripts/ — you can write custom Lua scripts',
        'Output format -oA is the professional standard — always save scans in all formats for reporting',
      ],
    },
  ],
  keyTools: ['nmap', 'masscan', 'hping3', 'p0f', 'Angry IP Scanner'],
  countermeasures: [
    'Strict Zero Trust architecture with default-deny firewall policies',
    'Implement stateful IDS/IPS with deep packet inspection and signature updates',
    'Configure proper egress filtering to prevent internal IP spoofing',
    'Deploy network honeypots (Honeyd, Cowrie) to detect internal scanning early',
    'Rate-limit connection attempts per source IP to mitigate rapid scanning',
    'Use TCP wrappers and iptables rules to restrict service access by source IP',
  ],
  commonPorts: [
    { port: 21, protocol: 'TCP', service: 'FTP', description: 'File Transfer Protocol — often allows anonymous login and cleartext credentials' },
    { port: 22, protocol: 'TCP', service: 'SSH', description: 'Secure Shell — prime target for brute force and key-based attacks' },
    { port: 23, protocol: 'TCP', service: 'Telnet', description: 'Unencrypted remote access — captures credentials in plaintext' },
    { port: 25, protocol: 'TCP', service: 'SMTP', description: 'Email routing — vulnerable to VRFY/EXPN user enumeration and relay' },
    { port: 53, protocol: 'UDP/TCP', service: 'DNS', description: 'Domain Name System — zone transfers, cache poisoning, amplification attacks' },
    { port: 80, protocol: 'TCP', service: 'HTTP', description: 'Unencrypted Web traffic — OWASP Top 10 attack surface' },
    { port: 110, protocol: 'TCP', service: 'POP3', description: 'Post Office Protocol — cleartext email retrieval' },
    { port: 143, protocol: 'TCP', service: 'IMAP', description: 'Internet Message Access Protocol — cleartext email access' },
    { port: 443, protocol: 'TCP', service: 'HTTPS', description: 'Encrypted web — often hides C2 traffic in legitimate TLS sessions' },
    { port: 445, protocol: 'TCP', service: 'SMB', description: 'Windows file sharing — EternalBlue, relay attacks, share enumeration' },
    { port: 3306, protocol: 'TCP', service: 'MySQL', description: 'MySQL database — default credentials and SQL injection target' },
    { port: 3389, protocol: 'TCP', service: 'RDP', description: 'Remote Desktop — brute force, BlueKeep, initial access broker target' },
    { port: 5432, protocol: 'TCP', service: 'PostgreSQL', description: 'PostgreSQL database — copy_to/copy_from can achieve RCE' },
  ],
  examTips: [
    'Know the TCP 3-way handshake: SYN → SYN-ACK → ACK. CEH tests this repeatedly.',
    'SYN scan (-sS) is "half-open" and stealthy because it never sends the final ACK. This is THE most tested scan type.',
    'XMAS scan (-sX) sets FIN+PSH+URG flags — only works against RFC-compliant TCP stacks (not Windows).',
    'NULL scan (-sN) sends no flags — also does not work against Windows. Know which scans fail on Windows.',
    'UDP scanning is slow because there is no handshake — closed ports respond with ICMP Port Unreachable, open ports may not respond at all.',
    'OS fingerprinting uses TTL values: Linux/Unix default TTL=64, Windows default TTL=128, Cisco default TTL=255.',
    'Nmap timing templates: -T0 (Paranoid), -T1 (Sneaky), -T2 (Polite), -T3 (Normal), -T4 (Aggressive), -T5 (Insane).',
    'The -D flag uses decoys, -f fragments packets, --source-port spoofs the source port — know all three for evasion questions.',
    'Idle scan (-sI) is the stealthiest scan — uses a zombie host. Your IP never appears in target logs.',
    'ACK scan (-sA) does NOT find open ports — it maps firewall rules. This is a common trick question on the exam.',
  ],
  realWorldScenarios: [
    'During an internal penetration test, you run nmap -sS -p- -T4 against a /24 subnet and discover an unpatched Windows Server 2008 R2 running SMB on port 445 vulnerable to MS17-010 (EternalBlue). This single finding provides SYSTEM-level access to the domain controller.',
    'You use masscan to rapidly scan the entire 10.0.0.0/8 internal network at 10,000 packets per second, identifying 340 live hosts in under 2 minutes. You then feed the results into nmap for detailed service enumeration.',
    'An IDS is flagging your scans. You switch to nmap -T1 --scan-delay 15s -f -D RND:5 to fragment packets, add decoy IPs, and slow the scan rate below the IDS threshold of 10 connections per second.',
    'You discover a PostgreSQL database on a non-standard port (5433) only because you ran a full -p- scan. The default top-1000 ports scan would have completely missed this critical service.',
  ],
  prerequisites: ['M02 — Reconnaissance data feeds directly into scanning targets'],
};
